# Copy to .env and edit values (DO NOT commit .env)
#
# Windows:
#   copy .env.example .env
# macOS/Linux:
#   cp .env.example .env

# Docker images (default: public images on Docker Hub)
# You can override these if you have custom images
RIZM_API_IMAGE=kabekenputer/keel-api:latest
RIZM_WEB_IMAGE=kabekenputer/keel-web:latest

# Initial admin (created only if no admin exists yet)
KEEL_BOOTSTRAP_ADMIN_EMAIL=admin@example.local
KEEL_BOOTSTRAP_ADMIN_PASSWORD=change-this-password

# Cookie security (for HTTPS, set to true)
KEEL_COOKIE_SECURE=false

# Domain mode only (for docker-compose.domain.yml)
# APP_DOMAIN=your-domain.com
# LETSENCRYPT_EMAIL=your-email@example.com

# AI Transform (optional)
# - Ollama (local)
# KEEL_OLLAMA_URL=http://host.docker.internal:11434
# KEEL_OLLAMA_MODEL=deepseek-r1:8b-llama-distill-q8_0
#
# - DeepSeek API (external)
# KEEL_DEEPSEEK_API_KEY=
# KEEL_DEEPSEEK_BASE_URL=https://api.deepseek.com
# KEEL_DEEPSEEK_MODEL=deepseek-chat
# KEEL_DEEPSEEK_TIMEOUT_SECS=300
# KEEL_DEEPSEEK_CONNECT_TIMEOUT_SECS=10
